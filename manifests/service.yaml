apiVersion: v1
kind: Service
metadata:
  name: nginx-poc-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: nginx-poc
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
